apply plugin: "war"

bootWar {
    mainClassName = "org.travelers.statistics.StatisticsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
